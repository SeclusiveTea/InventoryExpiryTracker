<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#40000000" /> <!-- Shadow color (adjust alpha as needed) -->
            <corners android:radius="10dp" /> <!-- Corner radius (adjust as needed) -->
        </shape>
    </item>
    <!-- Button background -->
    <item android:top="0dp" android:left="0dp" android:right="0dp" android:bottom="3dp">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Button color (adjust as needed) -->
            <corners android:radius="10dp" /> <!-- Corner radius (adjust as needed) -->
        </shape>
    </item>
</layer-list>